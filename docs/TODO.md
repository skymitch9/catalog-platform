# TODO — catalog-platform (ACTIVE work log)

> 🔄 **CONDUCTOR STATUS (~01:1x, session 6%):** MERGED→main: audio-count, covers-owner-key, backup-board.
> DONE reports: T-C,T-D,T-B,shelf. universe --friend fixed (320ff57, padhard already current). IN FLIGHT →
> ebook-site count; T-G picker; ingest-lock PID fix; MERGE agent (lent-to-person w/ authorized catalog.ts
> keep-both-imports fix, THEN universe-friend-fix). NEXT → GABI T2 dark; alias research; audits.
