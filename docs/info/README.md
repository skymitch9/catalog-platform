# catalog-platform — Information References

> **Audience:** Claude sessions. **Status:** TRACKED. Last verified: **2026-08-14**
> — the estate stack went LIVE overnight 2026-08-13→14 (auth Worker deployed +
> seeded, index Worker deployed with all three catalogs pushed, apex search +
> `/admin`, games enforcing, library in shadow). Operational runbooks live in
> `library_catalog/docs/access/` (`estate-auth.md`, `index-worker.md`,
> `themes.md`); the rows below describe the design docs.

Stable how-and-why reference for the platform repo. Current state and decisions
in flight live in [`../TODO.md`](../TODO.md); the system design in
[`../PLATFORM.md`](../PLATFORM.md).

| File | What it holds |
|---|---|
| [`status-pages.md`](status-pages.md) | The **four status pages** after the 2026-08-18 split (owner mock: "a health page… a page for data processing, a page for running the pipelines") — who owns what, the file/module map, and the probed-vs-pushed line that runs through the whole surface. ⚠️ Read its last section before editing any of them: `_headers` does **not** match by prefix (the three new pages nearly shipped with no CSP at all), a Hono CORS mount is **not implied by a route** (`/ops/ingestion` shipped correct, answered curl perfectly, and was unreachable from a browser), and predeploy pins **move with their subject** |
| [`agent-board-contract.md`](agent-board-contract.md) | The **pushed state blob** behind /status/agents and /status/processing (2026-08-18): the four sections (`agents`, `events`, `usage`, `processing`), field by field, and why it is a **doc rather than a schema** — the home-machine pusher for `processing` does not exist yet, so the Worker validates the envelope only (JSON object, ≤256 KB) and the renderers carry the tolerance. ⚠️ The two rules that fall out of that: **a missing number is not zero**, and the four causes of silence never collapse into one. Also the refusal table, and why `pushed_at` is the Worker's clock and never the pusher's |
| [`matching-thresholds.md`](matching-thresholds.md) | The 2026-08-12 re-measurement of `MIN_TITLE_SIMILARITY` (0.34) and the 0.7 spine floor against both production D1s; verdict on sharing them across books and games; the bare-series-name refusal rule |
| [`improvement-proposals.md`](improvement-proposals.md) | The 2026-08-13 estate-wide survey: ranked evidence-backed proposals, cross-catalog opportunities, remove/simplify candidates (games `play` + `sleeve_requirement` are dead schema), and the do-not-build list |
| [`index-worker-design.md`](index-worker-design.md) | The shared index Worker (`apps/index-worker/`), **LIVE at `index.heygabi.ai` since 2026-08-14** with all three catalogs pushed: two-tier join key (`work_fold` books-only with an empty-fold refusal for non-Latin titles; universe tier is the only cross-format join games enter), snapshot-replace push protocol, fixture-pinned fold, `/api/search` (ranked, human) vs `/api/lookup` (exact, identity). Reads are estate-members-only (§9 Q3 answered) |
| [`estate-auth-design.md`](estate-auth-design.md) | Estate-wide auth, **BUILT and LIVE at `auth.heygabi.ai` since 2026-08-14** (seeded; games enforcing, library shadow): identity stays local Firebase-token verification (both Workers pin project `audiobook-catalog`); membership is the dedicated auth Worker + D1 (`pending/approved/revoked` + per-catalog **visibility**, §4.5) consulted with a 10-min TTL cache; authorization stays per-app. The new-site inheritance contract with 8 conformance probes, the seed, the reversible rollout, threat model and failure-direction table |
| [`edit-audit-design.md`](edit-audit-design.md) | The cross-catalog **edit any detail + audit log** contract (2026-08-14): what crosses the repo boundary (audit semantics, key-move rules, identifier freeze) vs what stays home; per-catalog field tiers; the carry-with-attestation mechanism (library side BUILT + DEPLOYED 2026-08-13); how the audiobook overrides+rebuild model satisfies the same contract with git as its audit log; ⚠️ the silent audiobook-retitle key move (§3.4) and the A2/A3 fixes; build order and owner questions |
| [`estate-themes.md`](estate-themes.md) | The estate THEME SYSTEM (canonical asset in `sites/heygabi-home/public/assets/`, live on the apex): four user-selectable themes — classic (the apex's original look), apple, cyberpunk (from the audiobook site), retro (from the games app; ⚠️ actually muted vintage pop-art) — × light/dark on one `--et-*` token contract; the cog switcher (`hg_theme`/`hg_theme_page`/`hg_mode`, per-page since v2), per-site identity defaults (owner decision), the motion vocabulary, and the honest per-site fidelity costs |
| [`health-envelope.md`](health-envelope.md) | The one `{ ok, service, version?, time, detail }` envelope all four estate Workers' `/api/health` now answer (2026-08-14, additive — every pre-existing field kept at the top level too), per-worker before→after, `status.js`'s `detailOf()` fallback, and the deliberately-not-yet-done removal step |
| [`estate-scan-adoption.md`](estate-scan-adoption.md) | `assets/estate-scan.js` (2026-08-15, bookstore deploy): the canonical barcode/camera/ISBN-resolve/photo-capture module `<estate-search scan>` imports; what shipped LIVE vs. what's code-complete-but-unwired (shelf-photo identify — the vision endpoint contract to port, sized ~half a day); the "Add to Books" reuse of `library_catalog`'s own intake endpoint + its narrow CORS opening; the sized (not built) plan for `library_catalog` to eventually retire its own scanner onto this one |
| [`audiobook-auth-soak-2026-08-16.md`](audiobook-auth-soak-2026-08-16.md) | ⚠️ **The Phase 3 enforce gate: VERDICT = NOT ENOUGH EVIDENCE, do not flip, re-run in ~7 days.** The dated shadow-soak evidence pack for `ESTATE_CHECK` shadow → enforce: prod-lane soak measured at **1h 52m** (not the 4h the server suggests), **zero organic gate decisions observed** in a 5-minute tail sample (all 3 captured requests were the probes themselves), so the household false-denial count is **0 of 0 — unmeasured, not clean**. Three structural findings that outlive the thin data: shadow is **tail-only and unrecoverable** (no `[observability]`/D1/KV/AE binding anywhere), **2 of 25 gated actions are not instrumented at all** (`warning.modDelete`, `read.setSlot` — their silence can never be evidence), and ⚠️ **`reportGate()` fires from a `finally` block with no outcome field**, so the tail cannot separate a true regression from a gate merely agreeing with today's rules — the flip criterion is currently unfalsifiable. Static risk table naming which actors to exercise first, and the re-run command. ⚠️ **Its §9 re-run command is SUPERSEDED 2026-08-17** — see the row below |
| [`audiobook-auth-soak-rerun-2026-08-17.md`](audiobook-auth-soak-rerun-2026-08-17.md) | The dated addendum to the pack above: the **SOAK RECORDER** built 2026-08-17 (worker `8cdf7c88`), which closed all three of its blockers — `[observability]` retention so decisions survive (and, as its precondition, the cleartext `email` replaced by `email_hash` + `identity_class`), the `succeeded` outcome bit that makes the flip criterion falsifiable at last, and `read.setSlot` instrumented so **25 of 25** gated actions report. **Replaces §9 with a retrospective log QUERY** (exact command, the new fields, the counts that answer the bar) and records ⚠️ the credential gotcha that costs an hour otherwise: the Workers Observability API **refuses wrangler's OAuth token**, and wrangler 4.123 ships no observability subcommand — dashboard, or a proper API token. Also names the one thing instrumenting `read.setSlot` MAKES VISIBLE: an ordinary member renaming a read card will legitimately log `would_deny:true` + `succeeded:true`, a real predicted regression that is an owner decision, not a telemetry bug |
| [`ebook-viewer-phase1.md`](ebook-viewer-phase1.md) | The **as-built** record for the in-browser ebook viewer, phases 1a (`GET\|HEAD /api/ebook/:anchor/file`, this repo) and 1b (the vendored-pdf.js reader at `/read`, `audiobook_catalog`) — the shipped contract (verbs, statuses, headers, range table, CORS, the books-not-requests rate limit), ⚠️ the capability rule that the stream gates on **`vis_ebooks`** and never on `download` (and the stale comment that said otherwise), the one gate shared with the shelf and the header bug that extraction caused, the 393 MiB omnibus's deliberate `file_absent` 404, and **three measured deviations from the design** — chiefly ⚠️ `disableStream: true`, without which opening a 181 MiB PDF transfers all 189,930,310 bytes rather than the promised few hundred KB. §7 is blunt that **nobody has yet opened a real gated PDF signed in** |
| [`role-ladder.md`](role-ladder.md) | The audiobook site-roles federation extended (2026-08-16) from `admin\|moderator` to the full cumulative ladder `guest < member < contributor < moderator < admin < owner` (renamed mid-build from `viewer`/`reader` — the ⚠️ "estate member" vs. the `member` role clash is called out explicitly): the granting rule (strictly-beneath, moderator+ floor), `owner` as DB-only/API-immune, the two-owner-accounts `OWNER_EMAILS` change, the new `/site-roles/tree` capability-map endpoint, the `site_role_grant_log` D1 audit trail (0005) — and the ⚠️ firestore.rules limitation (member/contributor are UI/permission-map only until a separate, owner-gated rules change ships) |
| [`discord-bot-design.md`](discord-bot-design.md) | The estate Discord bot: setup, token custody, why it is its own Worker, the identity-link stance, and the full option space. §6 is conversational GABI phase A **as built** (@mention trigger, the Haiku ladder, the caps, the allowlist); §7 is the **continuity layer** (2026-08-17). ⚠️ §1.5's refusal of the **Message Content privileged intent** is the line every later section is built around |
| [`gabi-conversation-continuity.md`](gabi-conversation-continuity.md) | ⚠️ **THE SECOND SURFACE LANDED 2026-08-18.** The code moved to `packages/gabi-conversation/` (`@platform/gabi-conversation`) and is now read by the library site's chat panel as well; `apps/discord-worker/src/conversation.ts` re-exports it and keeps only the Discord-specific components, modal and sentences. ⚠️ **Do not add fields, limits or window logic to that file** — a constant added there is one the site panel silently does not have. §1.3 is a record rather than a prediction now, and **two of its five rows were wrong**: `space` is the catalog INSTANCE (`ESTATE_APP`), not a session id, and `person` is the `app_user` id because `firebase_uid` is nullable there. §1.4 is the **resume rule** — the panel holds its own transcript, so only turns from *other* `ref.cid`s are prepended, matched on the id and never on text. ⚠️ **The STORAGE is per-surface on purpose** (§3): a site chat turn must not depend on the Discord Worker's always-on socket. Unchanged below that: **the conversation-store SHAPE, deliberately not Discord's** — the owner's constraint was *"whatever we build we need to consider for when we update the chat button on GABI"*, so the record is designed for the library site's GABI panel to adopt, with surface-neutral fields separated from an opaque per-surface bag by a table and pinned by a test. Also: the 30-minute / 20-turn window and why **aged-out state is DELETED not archived**; the **four intent-free doors** with Discord's own wording quoted (⚠️ including the honest limit that a reply with **"ping on reply" switched OFF is invisible to her**, and that replies to slash-command answers never work); intents 513 → **4609** (`+DIRECT_MESSAGES`, unprivileged); the write-budget arithmetic showing continuity costs **≤400 Durable Object row writes/day (~2.5%)** by reusing the gateway object rather than adding a second one; and why the clarifying-question `custom_id` is **unsigned on purpose** while `moderation.ts`'s is MAC'd |
| [`gabi-confirm-lanes-design.md`](gabi-confirm-lanes-design.md) | **DESIGN ONLY** — the T2/T3 **confirm grammar**: propose → restate → confirm button → apply on borrowed authority → report. The proposal lives in the existing `pending.*` slot as a third `PendingChoice` kind (**zero new Durable Object writes**, arithmetic unchanged); ⚠️ the capability is checked **twice** (propose and press) because revocation beats everything; ⚠️ **the hard part is the restatement still being true at press time**, solved by **compare-and-set on `before`** with a worded 409 — the `firestore.rules` `hasAll` incident generalised (*validate the whole resulting state, never that your change is merely among the changes*); why this lane's nonce stays **unsigned** while `moderation.ts` MACs (*a MAC is what you use when you have nowhere to put the state*); TTLs 10 min T2 / 5 min T3 and why they are readability not safety; the verb lists with destination capabilities, ⚠️ **club deletion excluded as T4** and ⚠️ **T2 barred from the audiobook surface until it has an audit seam**; `changed_how='human'` argued as a reversible decision; the refusal set incl. `applyUncertain`; surface-neutral `Restatement` structure with Discord **and** panel-v2 rendering; 6 build phases with effort classes |
| [`gabi-bare-text-triggers-memo.md`](gabi-bare-text-triggers-memo.md) | **DECISION MEMO** (2 pages) — bare `heygabi` with no `@`, no reply, not a DM. ⚠️ **Corrects a superseded fact the estate's docs still carry**: the privileged-intent gate is no longer *100 servers* but **10,000 unique users** (Discord, June 2026), so for this estate there is **no gate at all** — one portal toggle, ~1 minute, fully reversible, nobody external to say no. ⚠️ **The intent CANNOT be scoped to channels** (checked against two Discord pages — no such mechanism exists), so the only mitigation is handler-side, and the memo states **exactly what "unlogged" can honestly promise** (not-stored/not-sent are true and checkable; *"the bytes never reach our servers"* is false) plus the mechanical guard it would need. The privacy delta in household terms: *"she cannot hear what you don't say to her"* (guaranteed by Discord) → *"she receives everything and ignores what isn't hers"* (guaranteed by us). **Recommendation: not yet** — the benefit is one saved `@`, DMs already solved the zero-`@` case, and deferring costs nothing — with the flip conditions written down. Owner's call |
