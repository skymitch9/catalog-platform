{
  "name": "catalog-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Platform-level docs, the heygabi.ai front door, the shared universe list both book catalogs read at build time, and the shared index Worker (apps/index-worker).",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "universes": "node tools/universes.mjs",
    "series-canon": "node tools/series-canon.mjs",
    "validate": "node tools/universes.mjs validate && node tools/universes.mjs fixtures && node tools/series-canon.mjs validate",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test:scripts && npm test --workspaces --if-present",
    "test:scripts": "node --test \"scripts/test/**/*.test.mjs\"",
    "backup:firestore": "node scripts/backup-firestore.mjs",
    "probe:estate": "node tools/estate-probes/run.mjs",
    "check:home": "node scripts/predeploy-check.mjs",
    "verify:home": "node scripts/predeploy-check.mjs --live",
    "deploy:home": "npm test && npm run check:home && npx wrangler pages deploy sites/heygabi-home/public --project-name=heygabi-home --branch=main --commit-dirty=false && npm run verify:home"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0"
  }
}
